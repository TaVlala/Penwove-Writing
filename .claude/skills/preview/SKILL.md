---
name: preview
description: Start both dev servers (frontend + backend) for Penwove. Use when the user wants to run or preview the app locally.
allowed-tools: mcp__Claude_Preview__preview_start
---

Start both Penwove servers:
1. Start `collab-backend` (port 3001)
2. Start `collab-frontend` (port 5173)
3. Report both server IDs and confirm they are running
